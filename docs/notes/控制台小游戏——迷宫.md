1. 实时读取输入：使用_kbhit()检测，返回状态（有、没有输入）非阻塞输入，程序读取到这一行不会像scanf一样停下来等待用户输入，而是只“看一眼”
2. 覆盖打印：打印总是在光标后面开始，所以要将光标不断移动到最顶上，使用SetConsleCursorPosition（施加对象的句柄（id，用GetStdHandle（））,目标位置 ）
3. 二维数组的内存：地址在内存中是连续的，过了一列的步长就倒下一列（左边是行，右边是列）
4. 实时刷新：使用while（1）死循环加Sleep（ms）（S大写，不然单位是秒）让程序休眠一会再执行
5. 读取一个字符：char c=_getch（），getch的安全版
6. esc退出：esc按键值为27